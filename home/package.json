{
  "name": "pi-mono-root",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "npm run --workspace=@my/toolchain build",
    "start": "npm run --workspace=@my/toolchain start"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
