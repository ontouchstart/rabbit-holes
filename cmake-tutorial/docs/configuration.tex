% configuration.tex
\section{Build System Configuration}

\subsection{CMakeLists.txt Structure}
Based on the logs provided for the {\tt add\_executable}, {\tt add\_library}, and {\tt target\_link\_libraries} modules, the build system utilizes modern CMake syntax (specifically {\tt target\_sources}). The configuration corresponds to the following logic:

\begin{verbatim}
cmake_minimum_required(VERSION 3.31)
project(tutorial)

Add Executable
add_executable(main)

Add Library
add_library(answer)

Target Link Libraries
target_link_libraries(main answer)
\end{verbatim}

\begin{verbatim}

Assigning sources to targets (as seen in logs)
target_sources(main
PRIVATE
src/main.cc
)

target_sources(answer
PRIVATE
lib/answer.cc
PUBLIC
FILE_SET HEADERS
BASE_DIRS
include
FILES
include/answer.h
)
\end{verbatim}

\subsection{Generated Makefile}
The generated Makefile confirms proper CMake configuration and path handling on the Windows Server environment.

\begin{verbatim}
default_target: all
.PHONY : default_target

CMAKE_COMMAND = "C:/Program Files/CMake/bin/cmake.exe"
CMAKE_SOURCE_DIR = D:/a/rabbit-holes/rabbit-holes/cmake-tutorial/generators/UNIX
CMAKE_BINARY_DIR = D:/a/rabbit-holes/rabbit-holes/cmake-tutorial/generators/UNIX/build
SHELL = /bin/sh
\end{verbatim}
