services:
  pi:
    build:
      context: .
      dockerfile: Dockerfile
    stdin_open: true
    tty: true
    volumes:
      - ./models.json:/root/.pi/agent/models.json
      - ./settings.json:/root/.pi/agent/settings.json
      - ./vibe/sessions:/root/.pi/agent/sessions
      - ./vibe:/vibe
